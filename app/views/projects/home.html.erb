<h1>Navigator</h1>

<h2>Existing Projects</h2>
<%@projects.each do|project| %>
<h4><%=link_to project.name, project_path(project)%></h4>
<%end%>
<br>
<%= button_to "Start a new project", new_project_path%>
